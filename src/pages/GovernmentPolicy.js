import { Layout } from '../Shared/Layout';
import { GovernmentPolicy } from '../GovernmentPolicy';

export const GovernmentPolicyPage = () => {

  return (
    <Layout>
      <GovernmentPolicy/>
    </Layout>
  );
};
